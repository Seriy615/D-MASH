# D-MASH: Secure P2P Messenger with Blind Routing

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

D-MASH (Decentralized Anonymous Secure Hashing) — это экспериментальный P2P-мессенджер, разработанный с максимальным упором на безопасность и приватность. В отличие от традиционных мессенджеров, D-MASH не только шифрует содержимое ваших сообщений (End-to-End Encryption), но и скрывает **метаданные** о том, кто, с кем и когда общается, благодаря протоколу слепой маршрутизации.

![D-MASH Logo](D-MASH/client/frontend/logo.png)
![UI Screenshot](D-MASH/chatUI.png)

## Ключевые идеи

Проект построен на трех уровнях защиты, обеспечивающих беспрецедентную приватность:

1.  **🔒 Уровень пользователя (E2EE):** Полное сквозное шифрование с использованием временного храповика (T-Ratchet). Ключ шифрования динамически генерируется из общего секрета (ECDH) и текущего времени, обеспечивая Perfect Forward Secrecy. Даже если долгосрочные ключи пользователя скомпрометированы, старая переписка останется в безопасности.

2.  **🌐 Уровень сети (Анонимность):** Протокол слепой маршрутизации (Blind Routing). Узлы в сети пересылают зашифрованные пакеты, не зная ни конечного получателя, ни исходного отправителя. Они видят только зашифрованную инструкцию для следующего шага, что делает анализ трафика практически невозможным.

3.  **🛡️ Уровень инфраструктуры (Надежность):** Идентичность каждого узла сети защищена Proof-of-Work. Для создания нового узла требуется выполнить сложные вычисления, что делает сеть устойчивой к Sybil-атакам (созданию множества фейковых узлов).

## ✨ Ключевые особенности (Key Features)

-   **Сопротивление анализу метаданных:** Благодаря слепой маршрутизации, наблюдатель не может определить, кто с кем общается.
-   **End-to-End шифрование:** Используется современная криптография (`XSalsa20-Poly1305`, `Curve25519`, `Ed25519`) из библиотеки PyNaCl.
-   **Устойчивость к Sybil-атакам:** Proof-of-Work для идентификаторов узлов.
-   **Маскировка трафика:** Все сетевые пакеты имеют фиксированный размер и отправляются с постоянным интервалом (даже "пустые"), чтобы скрыть реальную активность.
-   **Полная децентрализация:** Отсутствие центральных серверов. Узлы общаются напрямую друг с другом.
-   **Зашифрованное локальное хранилище:** Вся история сообщений на диске шифруется ключом, производным от вашего пароля.
-   **Простое развертывание:** Проект полностью контейнеризирован с помощью Docker.

## 🛠️ Техническая архитектура

### Бэкенд
-   **FastAPI:** Высокопроизводительный веб-фреймворк для API.
-   **WebSockets:** Для P2P-коммуникаций между узлами.
-   **PyNaCl:** Основа для всех криптографических операций (E2EE, подписи, хеширование).
-   **Blake3:** Быстрый и безопасный алгоритм хеширования, используемый в протоколе маршрутизации.
-   **aiosqlite:** Асинхронная работа с локальными базами данных.

### Фронтенд
-   **Vanilla JavaScript, HTML5, CSS3:** Легковесный и быстрый интерфейс без зависимостей от фреймворков.

### Модель безопасности
1.  **Аутентификация пользователя:** Ключи генерируются из пароля с помощью **Argon2id** — золотого стандарта хеширования паролей.
2.  **Шифрование локального хранилища:** База данных пользователя шифруется симметричным ключом (XSalsa20-Poly1305).
3.  **Безопасность транспорта:** TLS/SSL для всех HTTP и WebSocket соединений.
4.  **Шифрование сообщений:** E2EE с использованием T-Ratchet для обеспечения Perfect Forward Secrecy.
5.  **Защита метаданных:** "Слепое хранилище" (Blind Storage) для таблиц маршрутизации, где ключи хешируются с солью из RAM, а значения шифруются для самого узла.

## 🚀 Установка и запуск

Проект разработан для запуска в Docker.

### Предварительные требования
-   Docker
-   Docker Compose

### Запуск
1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Seriy615/D-MASH.git
    cd D-MASH/client
    ```

2.  **Запустите Docker Compose:**
    Файл `user-docker-compose.yml` уже настроен для запуска 4 независимых узлов.
    ```bash
    docker-compose -f user-docker-compose.yml up --build
    ```
    При первом запуске узлы начнут "майнить" свои Identity (Proof-of-Work). Это может занять от 30 секунд до нескольких минут в зависимости от мощности вашего процессора. Следите за логами.

3.  **Доступ к узлам:**
    После завершения инициализации узлы будут доступны в вашем браузере по адресам:
    -   **Узел 1:** `https://localhost:8001`
    -   **Узел 2:** `https://localhost:8002`
    -   **Узел 3:** `https://localhost:8003`
    -   **Узел 4:** `https://localhost:8004`

    *Примечание: Поскольку используются самоподписанные SSL-сертификаты, ваш браузер выдаст предупреждение о безопасности. Его необходимо принять.*

## 💬 Как пользоваться

1.  **Создайте личность:** Откройте два узла в разных вкладках браузера (например, `https://localhost:8001` и `https://localhost:8002`). На каждом узле введите уникальный логин и пароль (например, `user1`/`pass1` и `user2`/`pass2`).

2.  **Скопируйте ID:** После входа в систему, в левом верхнем углу будет отображаться ваш уникальный ID. Кликните на него, чтобы скопировать в буфер обмена.

3.  **Соедините узлы:**
    -   На узле 1 (`:8001`), нажмите кнопку `+ CONNECT NODE`.
    -   В появившемся окне введите адрес для подключения ко второму узлу: `node2:9001` и нажмите `CONNECT`.
    -   Теперь узлы знают о существовании друг друга и могут обмениваться маршрутной информацией.

4.  **Начните чат:**
    -   На узле 1, вставьте ID узла 2 в поле "Paste Peer ID" и нажмите `OPEN CHAT`.
    -   Сделайте то же самое на узле 2, вставив ID узла 1.

5.  **Отправьте сообщение:**
    -   Теперь вы можете отправлять зашифрованные сообщения. При отправке первого сообщения будет запущен протокол обнаружения маршрута (`PROBE`). Доставка может занять несколько секунд. Последующие сообщения будут доставляться почти мгновенно по установленному туннелю (`DATA`).

## 🗺️ Дальнейшие планы (Roadmap)

-   [ ] **Real-time UI:** Переход от опроса (polling) к WebSockets для мгновенного обновления интерфейса.
-   [ ] **Масштабируемый роутинг:** Изучение и внедрение DHT (например, Kademlia) для эффективной работы в сетях с тысячами узлов.
-   [ ] **Передача файлов:** Реализация безопасной E2EE передачи файлов.
-   [ ] **Групповые чаты:** Разработка протокола для безопасных групповых коммуникаций.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл `LICENSE` для получения дополнительной информации.
