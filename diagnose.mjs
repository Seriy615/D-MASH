import { chromium } from 'playwright';
const browser=await chromium.launch({headless:true}); const c=await browser.newContext(); const p=await c.newPage(); const logs=[]; p.on('console',m=>logs.push([m.type(),m.text()])); p.on('pageerror',e=>logs.push(['pageerror',e.message])); const r=await p.goto('https://messenger.d-mash.ru/not_messenger/',{waitUntil:'networkidle',timeout:30000}); await p.waitForTimeout(5000); console.log(JSON.stringify({status:r.status(),url:p.url(),globals:await p.evaluate(()=>({release:window.DMASH_RELEASE?.id,node:!!window.NodeManager,device:!!window.DeviceRoot,core:!!window.Core,controller:!!navigator.serviceWorker?.controller,body:document.body.innerText.slice(0,300)})),logs},null,2)); await browser.close();
